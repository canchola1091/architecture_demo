# GetX Architectures Demo

Este proyecto demuestra diferentes arquitecturas y gestores de estado en Flutter:

- MVVM (con Provider)
- BLoC
- MobX
- GetX

### Para ejecutar:

1. `flutter pub get`
2. `flutter run`

### Generar código para MobX:

```bash
flutter packages pub run build_runner build